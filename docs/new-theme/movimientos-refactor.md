1. I want to refactor Movimientos grilla as defined in mock 'docs\new-theme\finanzas-rediseno-propuesta.html'
2. I want to use material-react-grid, as in other pages, instead of the current implementation.
3. With the new grid, I want primary rows by day, and secondary rows for each movement, as defined in the mock.
4. Hovering over a secondary row should show edit button, like in the mockup
5. Edition should be inline. The mockup shows a guide, ideally we could reuse the existing inline edition components.
6. Clicking on agregar should add a top row for the new movement, with inline edition enabled.
7. We should still have a check-all checkbox in the header, and a checkbox for each movement, as in the current implementation.
