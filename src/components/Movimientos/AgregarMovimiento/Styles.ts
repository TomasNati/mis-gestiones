import { SxProps } from '@mui/material';

interface Styles {
  container: SxProps;
}

const styles: Styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
  },
};

export { styles };
